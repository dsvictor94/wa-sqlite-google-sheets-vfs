# Demo

The GitHub Pages demo lives in `demo/main.ts` and is built into `demo/dist`.

It asks for a Google API key and OAuth client ID, requests Sheets access, creates a spreadsheet, registers the Google Sheets VFS, inserts one SQLite row, and selects it back.
